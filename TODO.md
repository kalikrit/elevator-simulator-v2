# TODO

Известные задачи и проблемы, отложенные осознанно.

---

## 🔴 Известные проблемы

Пока пусто.

---

## 🟡 Технический долг

- `TickResult.justFinished` вычисляется, но не используется. Либо задействовать, либо убрать.
- `getElevators()` возвращает ссылку на внутренний массив с типом `readonly`. В рантайме можно мутировать. Смягчить через `Object.freeze` или копию — если понадобится.
- Composable `useElevatorSystem` возвращает `elevators` как `shallowRef`, но заменяет ссылку на массив каждый кадр. Если начнёт тормозить — рассмотреть `reactive` + мутацию in-place.
- Симуляция в реальном времени (`requestAnimationFrame`) даёт небольшой разброс метрик между запусками. Для детерминированных результатов — фиксированный `deltaMs` (например, 16 мс/кадр). **Приоритет: высокий** — без этого сравнение алгоритмов нечестное.

---

## 🟢 Отложенные фичи (не в текущем scope)

- **Плотный сценарий** с 15–20 вызовами, чтобы задействовать все 4 лифта и получить статистически значимую разницу между алгоритмами. На текущем сценарии из 5 вызовов разница в пределах шума. **Приоритет: высокий.**
- Дополнительные сценарии.
- Третий алгоритм управления.
- Сравнение более чем двух запусков (история).
- Настройка параметров (этажи, количество лифтов, скорость) через UI.
- Пассажиры как сущности с весом / приоритетом.
- Сохранение результатов между сессиями.
- Мобильная адаптация (сейчас есть простой `@media < 1000px`).
- Улучшение алгоритма `nearest`: учитывать вероятность попутных вызовов. Сейчас он этого не делает — и это часть дизайна, потому что контраст с `totalTime` показывает разницу алгоритмов.
- Плавное движение лифтов между этажами. Сейчас лифт «прыгает» с этажа на этаж через `Math.round`. Нужна переделка структуры: лифты вне строк `Floor`, `position: absolute` в общем контейнере.
- Return to base: лифты возвращаются на 1 этаж после N минут простоя. Нужен счётчик, обработка в `tick`, сброс при вызове.
- Round-robin tie-breaking в `pickElevator` — при равенстве цен выбирать наименее занятый лифт.
- Кастомный домен (если понадобится).
- Прогон тестов на нескольких версиях Node в CI (сейчас только Node 20).

---

## ✅ Сделано

### Проектирование
- [x] Шаг 0: требования и use cases
- [x] Шаг 1: модель предметной области
- [x] Шаг 2: границы модулей
- [x] Шаг 3: контракты модулей
- [x] Шаг 4: scaffold проекта + репозиторий

### Домен (52 теста)
- [x] Шаг 5: `queue` + тесты (16)
- [x] Шаг 6: `selector` + тесты (11)
- [x] Шаг 7: `metrics` + тесты (10)
- [x] Шаг 8: `elevatorSystem` + тесты (16)

### Composables
- [x] Шаг 9: `useElevatorSystem`, `useScenarioRunner` + smoke-тест

### UI
- [x] Шаг 10: UI-компоненты + техно-стиль
  - `Floor.vue`, `Floors.vue`, `ControlsPanel.vue`, `AlgorithmDescription.vue`, `MetricsPanel.vue`, `MetricsComparison.vue`, `ElevatorSimulator.vue`
- [x] Сравнение двух алгоритмов на одном сценарии
- [x] Повышение точности отображения метрик до 2 знаков после запятой

### Документация
- [x] `README.md` с описанием проекта, стека, архитектуры и процесса
- [x] `TODO.md` с известными задачами

### CI/CD
- [x] Шаг 11: деплой на GitHub Pages через GitHub Actions
  - Автоматический прогон тестов перед деплоем
  - Badge статуса деплоя в README

### Исправленные проблемы
- [x] Проблема №1: завышенное ожидание для вызовов с этажа лифта
- [x] Проблема №2: сортировка очереди не разделяет сегменты up/down
- [x] Проблема №3: лифты возвращались на 0 после каждой поездки, искажая метрики

### Анализ
- [x] Проверка сравнения алгоритмов на реальных метриках
  - Результат: на сценарии из 5 вызовов `nearest` обошёл `totalTime` по всем метрикам
  - Вывод: разница в пределах шума, нужен плотный сценарий для честного сравнения