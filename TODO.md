# TODO

Известные задачи и проблемы, отложенные осознанно.

---

## 🔴 Известные проблемы (найдены при smoke-тесте)

### 1. Завышенное среднее ожидание для вызовов с этажа, где лифт уже стоит

**Симптом:** при первом шаге сценария `{ from: 0, to: [3, 10, 17, 24] }` лифт уже на 0 этаже, но `metrics.onCallMade` фиксирует вызов, а `onElevatorArrived` для этажа 0 никогда не срабатывает. Ожидание записывается позже — когда другой лифт случайно вернётся на 0.

**Результат:** среднее ожидание 26 с вместо ожидаемых ~5–10 с; макс 56 с.

**Решение (вариант 2, выбран):** в `ElevatorSystem.requestTrip` при `currentFloor === from` сразу вызывать `metrics.onElevatorArrived(elevator, from, now)`.

**Приоритет:** высокий (искажает метрики, а метрики — суть приложения).

### 2. Сортировка очереди не разделяет сегменты up/down

**Симптом:** при `direction = 'up'` и очереди `[20, 0]` новый этаж 22 пушится в конец → `[20, 0, 22]`. Лифт едет 0 → 20 → 0 → 22 вместо 0 → 20 → 22 → 0.

**Результат:** лишние остановки, лишний пробег (11 остановок вместо 12, 132 этажа вместо ~80).

**Решение:** в `queue.addTarget` сортировать с учётом двух сегментов:
- `direction = 'up'`: сначала все этажи `> current` по возрастанию, потом все `< current` по убыванию.
- `direction = 'down'`: наоборот.

**Приоритет:** средний (влияет на качество метрик, но не ломает логику).

---

## 🟡 Технический долг

- `TickResult.justFinished` вычисляется, но не используется. Либо задействовать, либо убрать.
- `getElevators()` возвращает ссылку на внутренний массив с типом `readonly`. В рантайме можно мутировать. Смягчить через `Object.freeze` или копию — если понадобится.

---

## 🟢 Отложенные фичи (не в текущем scope)

- Дополнительные сценарии.
- Третий алгоритм управления.
- Сравнение более чем двух запусков (история).
- Настройка параметров (этажи, количество лифтов, скорость) через UI.
- Пассажиры как сущности с весом / приоритетом.
- Сохранение результатов между сессиями.
- Мобильная адаптация.

---

## ✅ Сделано

- [x] Шаг 0: требования и use cases
- [x] Шаг 1: модель предметной области
- [x] Шаг 2: границы модулей
- [x] Шаг 3: контракты
- [x] Шаг 4: scaffold проекта + репозиторий
- [x] Шаг 5: домен `queue` + тесты (11)
- [x] Шаг 6: домен `selector` + тесты (11)
- [x] Шаг 7: домен `metrics` + тесты (10)
- [x] Шаг 8: домен `elevatorSystem` + тесты (16)
- [x] Шаг 9: composables + smoke-тест